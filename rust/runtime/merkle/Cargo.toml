[package]
name = "runtime-merkle"
version = "0.1.0"
edition = "2021"

[lints]
workspace = true

[dependencies]
runtime-proto = { path = "../proto" }
runtime-random = { path = "../random" }
sha2 = { version = "0.10.8", features = ["asm"] }
